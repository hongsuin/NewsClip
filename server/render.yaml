services:
  - type: web
    name: newsclip-api
    env: node
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: PORT
        value: 8080
      - key: DB_PATH
        value: ./dailyclip.db
      - key: JWT_SECRET
        generateValue: true
